[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for any path that isn't a real file, so client-side
# routes (e.g. /eventos/noite-da-sopa) don't 404 on refresh or direct link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
